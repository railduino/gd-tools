##
## SSL settings (Dovecot has sane defaults)
##

ssl = required

ssl_server_cert_file = {{.CertDir}}/fullchain.pem
ssl_server_key_file = {{.CertDir}}/privkey.pem

ssl_server_dh_file = {{.DHParamsPath}}

